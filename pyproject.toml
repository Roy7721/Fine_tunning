[project]
name = "fine-tunning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Roy7721", email = "s2010724129@ru.ac.bd" }
]
requires-python = ">=3.14"
dependencies = [
    "gptqmodel>=7.3.6",
    "torchvision>=0.29.0",
]

[project.scripts]
fine-tunning = "fine_tunning:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
